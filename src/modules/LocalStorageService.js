
export default {
    LocalStore: {
        token: localStorage.getItem('token'),
        getUserDetails(token){
            // Fetch User Details from Server and set User Details in LocalStorage
            //localStorage.getItem()
            //details: {}
        }
    }

}